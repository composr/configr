authentication:
  enabled: false
  proxy:
    host:
    port:
authorization:
  enabled: false
  roles:
    read:
    write:
    delete:
cors:
  allowedOrigins: "*"
springdoc:
  packagesToScan: com.citihub.configr.namespace, com.citihub.configr.version, com.citihub.configr.query
  pathsToMatch: /**
mongodb:
  uri: mongodb://localhost:27017/?ssl=false
  db: configr
  auth_db: admin
  username:
  password:
  mongo_keystore_path:
  mongo_keystore_password:
azure:
  activedirectory:
    client-id: 11111111-1111-1111-1111-1111111111111111
    app-id-uri: <your-app-ID-URI>